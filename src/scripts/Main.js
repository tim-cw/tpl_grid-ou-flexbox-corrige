/** Componsante Main de TimTools */
export default class Main {
  constructor(element) {
    this.init();
  }

  /**
   * Méthode d'initialisation
   */
  init() {
    console.log('allo le curieux, on va jouer ici dans quelques semaines');
  }
}
new Main();
